name: prism_mobile
description: "PRISM Insurance Companion — Android Mobile Application"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.0
  go_router: ^14.8.0
  http: ^1.3.0
  intl: ^0.20.2
  file_picker: ^12.3.0
  image_picker: ^1.2.3
  pdf: ^3.13.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
